val name = "anu"
val age = 32
val msg = s"HI $name otday your $age you are gettting old"

val age = 12
if(age > 18) "major" else "minor"

if(age > 13 && age < 32) "jj products are free"
else if(age > 33 && age < 34) "mobile is free"

else s"age is less thn 13 and your age is $age"